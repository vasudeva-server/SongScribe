---
description: Apply the java.md/kotlin.md formatting rules to files (uncommitted changes, a path, or a package)
argument-hint: "[optional: file path, directory path, or dotted package name (e.g. songscribe.ui.renderer)]"
---

Invoke the Workflow tool with `{scriptPath: ".claude/workflows/format.js", args: "<ARGUMENTS>"}`.

Pass `args` as the raw `$ARGUMENTS` string, or omit it (null) if no argument was given.

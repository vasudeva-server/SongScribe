### 13B. `SelectionTest` (15 tests)

| file · method | behavior | genuine e2e? | matrix coverage (§ · verdict) | verdict | action |
|---|---|---|---|---|---|
| `SelectionTest · BasicSelection.testClickEmptySpaceDeselects` | Click empty area in select mode clears selection (dispatches real mouse click, asserts `getSelectionSize()==0`) | yes | §6A `clearSelection` — adequate; `ScoreInputHandler.keyPressed(ESCAPE)` deselect path — §7A wrong-level | keep | Genuine: requires real mouse dispatch through `LineComponent.mouseClicked` → `LineSelectionHandler.handlePress` → `clearSelection` pipeline; the entire routing chain is the risk |
| `SelectionTest · RangeSelection.testShiftClickExtendsSelection` | Shift-click on note 3 extends range from note 1 to note 3 (size==3) | yes | §7C `LineSelectionHandler.handleClick shift-extend` — adequate (e2e) | keep | Genuine: shift-click dispatch through `ScoreInputHandler` → `LineComponent` → `extendSelectionTo` pipeline; §7C records this as adequate e2e |
| `SelectionTest · RangeSelection.testShiftClickShrinksSelection` | Shift-click closer note shrinks range (size==2, note 3 deselected) | yes | §7C `LineSelectionHandler.handleClick shift-extend` — adequate (e2e) | keep | Genuine e2e (shift+click wiring); sequential with prior test; size assertion is exact — no weakness |
| `SelectionTest · RangeSelection.testDragSelect` | Rubber-band drag selects notes via `pressMouse`/`moveMouse`/`releaseMouseButtons`; asserts `isGreaterThanOrEqualTo(3)` | yes (method is genuinely e2e) | §7C `LineSelectionHandler.handleDrag` — **inadequate** (§7C line 2368 and §6A line 1979 both flag this: should be `isEqualTo(3)`) | keep (but inadequate assertion) | Genuine: rubber-band drag requires real AWT mouse dispatch. However the assertion is weak (`>=3`) — a clamping regression that selects too many elements would pass. §7C explicitly says change to `isEqualTo(3)`. Record as **inadequate** |
| `SelectionTest · LineSelection.testClickInStaffHeaderSelectsLine` | Click in staff clef region activates line selection (`isLineSelected(0)==true`) | yes | §6A `isLineSelected` — **wrong-level** (the coordinator's cross-line guard needs a unit test; this e2e is the only coverage); §7C `LineSelectionHandler.handlePress` routing — adequate | keep | Genuine: requires real click dispatch through `LineSelectionHandler` staff-header branch; also provides the only coverage for `isLineSelected` coordinator delegation (matrix records it as wrong-level for the unit gap, but the e2e itself is valid) |
| `SelectionTest · LineSelection.testClickPastElementsDoesNotSelectLine` | Click past last element does not select line (`isLineSelected==false`) | yes | §7C `LineSelectionHandler.handlePress` routing — adequate (e2e) | keep | Genuine: exercises the X-boundary guard in the staff-line hit-test branch of `LineSelectionHandler` — requires real coordinates and real dispatch |
| `SelectionTest · BarlineSelection.testBreathMarkDisablesDurationsAndBarlines` | Drag-select a breath mark; assert all duration actions disabled, barline action disabled, breath-mark action enabled | ambiguous → yes | §5A `enableFromSelectionSize`/`enableFromRestMode` — **missing** (unit); §5B `ElementTypeAction.appliesTo BREATH_MARK` — **missing** (unit) | keep | The drag-selection itself requires real mouse dispatch. The toolbar enablement asserted here (`isEnabled()` queried directly on `UIAction` instances without robot interaction) is unit-testable in isolation — §5A records the enablement predicates as missing unit tests. However, the combination of breath-mark drag-select + enablement reflection in one test is the wiring risk; the unit tests for predicates are **separately** missing. Keep as e2e coverage of the integration; note the unit predicates still need separate unit tests. |
| `SelectionTest · ClickAndMode.testAltClickEmptySpaceEntersSelectWithNoSelection` | Alt+click empty space from edit mode: mode becomes SELECT, selection size 0 | yes | §7A `ScoreInputHandler.keyPressed(ALT)` — **missing**; §7C `LineComponent.mousePressed` alt-click routing — **wrong-level** | keep | Genuine: requires real alt+click event dispatch through `ScoreInputHandler` → `LineComponent.mousePressed` alt-click branch; the routing is the integration risk. Unit gap for the alt-key branch is separately noted in §7A/7C. |
| `SelectionTest · ClickAndMode.testClickNoteInSelectModeSelectsIt` | Click note in select mode: `getSingleSelectedElement()` equals that note, mode stays SELECT | yes | §7C `LineSelectionHandler.handlePress` routing — adequate (e2e) | keep | Genuine: requires real click dispatch; asserts both model state and mode — wiring between `mouseClicked` → `handlePress` → `setSelectionFromClick` is the risk |
| `SelectionTest · ClickAndMode.testAltClickInSelectModeSameAsClick` | Alt+click in select mode selects element (same as plain click) | yes | §7C `LineSelectionHandler.handlePress` routing — adequate (e2e) | keep | Genuine: verifies that alt+click in SELECT mode does not switch mode but still selects; requires real alt+click dispatch |
| `SelectionTest · ClickAndMode.testReleaseWithoutDragKeepsSelection` | Click note, verify selection persists (no spurious deselect on mouseRelease) | yes | §7C `NoteDragHandler.handleRelease no-drag` — **missing** (unit for collapse path); §6A `getSelectionSize` — adequate | keep | Genuine: the "no drag occurred → selection preserved" contract involves real mouse press+release dispatch through `NoteDragHandler.handlePress`/`handleRelease` wiring; the no-drag collapse path is separately flagged as missing unit test in §7C |
| `SelectionTest · Drag.testAltDragTiedNoteFromEditMode` | Alt-drag tied note from edit mode: mode → SELECT, source and partner tied note both moved to `targetSp` | yes | §7C `NoteDragHandler.handleRelease mutation emission` — adequate; `handlePress group building tie-chain expansion` — adequate | keep | Genuine: requires real alt+drag event dispatch; also tests cross-component wiring (edit mode → drag → mutation → mode switch) that cannot be mocked away |
| `SelectionTest · Drag.testMultiNoteShiftSelectAndDrag` | Shift-select 3 notes, drag via robot, verify all 3 move by -2 staff positions | yes | §7C `NoteDragHandler.handleDrag group clamping` — adequate; `handlePress group building` — adequate | keep | Genuine: multi-note drag requires real robot drag; the group-delta arithmetic across 3 elements is the integration risk |
| `SelectionTest · Drag.testTieChainExpansionDuringMultiNoteDrag` | Select fermata+tied_1, drag tied_1; tied_2 (outside selection) also moves (+2 sp) — tie-chain expansion | yes | §7C `NoteDragHandler.handlePress group building: multi-selection + tie-chain expansion` — adequate | keep | Genuine: tie-chain expansion during drag is the cross-component wiring risk (selection + `NoteDragHandler` tie-expansion + mutation) that cannot be meaningfully mocked |
| `SelectionTest · Drag.testDragWithNoteAndRestSelectionMovesOnlyNote` | Select rest+note, drag note; only note moves, rest unchanged, both still selected | yes | §7C `NoteDragHandler.handleDrag group clamping` — adequate; §6A `getSelectionSize` — adequate | keep | Genuine: tests rest-filtering in multi-note drag — that rests are excluded from movement while remaining selected requires the full drag pipeline |

---

**Tally:** keep 15 (of which 1 is inadequate — `testDragSelect` weak `>=3` assertion), wrong-level 0, redundant 0, orphan 0.

**Notes.** All 15 methods exercise real AWT mouse dispatch (`pressMouse`, `moveMouse`, `releaseMouseButtons`, `clickAt`, `altClickAt`, `shiftClickAt`, `altDrag`, `dragNote`) or real mode-switch wiring that depends on the full `ScoreInputHandler → LineComponent → LineSelectionHandler / NoteDragHandler` pipeline — they are genuine e2e cases by the rubric. The one actionable finding is `testDragSelect`: both §6A (line 1979) and §7C (line 2368) recorded its `isGreaterThanOrEqualTo(3)` assertion as inadequate; the fix is `isEqualTo(3)`. The toolbar-enablement assertions in `testBreathMarkDisablesDurationsAndBarlines` call `UIAction.isEnabled()` directly (no robot button interaction) — those predicates are unit-testable separately (§5A records the missing unit tests), but the drag-select-then-check-toolbar wiring is still genuine e2e integration. No orphan tests were found; all fixtures (`selection1`, `selection2`) are actively used, and the header javadoc claim that this file replaces older test files is consistent with the covered behaviors.


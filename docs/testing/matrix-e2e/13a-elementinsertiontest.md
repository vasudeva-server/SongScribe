### 13A. `ElementInsertionTest` (17 tests)

| file · method | behavior | genuine e2e? | matrix coverage (§ · verdict) | verdict | action | done |
|---|---|---|---|---|---|---|
| `GraceNoteCancellation.testEscapeCancels` | Real keyboard VK_ESCAPE dispatched → `GraceModeManager.keyPressed` cancels grace mode; also asserts `DISABLE_IN_GRACE_MODE` toolbar state during grace mode (glissando/rest/grace disabled). | yes | §6C: `keyPressed` Escape → adequate e2e; toolbar state → adequate e2e (partial via assertAll). | keep | — | — |
| `GraceNoteCancellation.testDragLeftCancels` | Real robot drag to `getCancelThresholdPx()` offset → grace note removed, mode inactive. Time-based gesture requires real mouse timing (uses `Utils.sleep(MIN_DRAG_MILLIS)`). | yes | §6C: drag-left cancel path → adequate e2e. | keep | — | — |
| `GraceNoteCancellation.testSamePitchError` | Click grace note at pitch 0; second click at same pitch triggers error dialog; asserts element count restored and mode inactive. Requires real `JOptionPane` dispatch via `JOptionPaneFinder`. | yes | §6C: same-pitch error → adequate e2e. | keep | — | — |
| `GraceNoteDragConnect.testDragConnectToStandaloneNote` | Real press-drag to `getConnectThresholdPx()` offset pairs grace note with host; asserts grace type, glissando type CONNECTED, count+1, mode inactive. Time-based gesture. | yes | §6C: drag-right connect path → adequate e2e. | keep | — | — |
| `GraceNoteDeletion.testReplaceHostWithPitchedNotePreservesGlissando` | In edit mode, click at host's X with different Y (new pitch); asserts count unchanged, host becomes MINIM, grace glissando still CONNECTED. All assertions are pure model state in `PreviewElementManager.modifyExistingElement` copy-constructor branch. | no | §7C: `modifyExistingElement` decoration preservation → adequate unit (`PreviewElementManagerAttachmentTest`). Glissando preservation is via copy ctor (same path). | wrong-level | Write unit test: construct line with paired-grace + host; call `handleClick` with mocked `LineComponent` at host X but different pitch; assert glissando preserved. | ✅ |
| `GraceNoteDeletion.testDeleteHostRemovesBoth` | SELECT mode; click host; VK_DELETE → `handleDelete` routes to `deleteSelection` (because `isHostOfPairedGraceNote` is true); asserts count -2. Exercises the `handleDelete` paired-grace-note fallback branch, not just `deleteNote`. | yes | §7A: `handleDelete on element selection with paired-grace-note at selection start: falls back to per-element loop` → missing unit coverage. Real key dispatch + selection routing through Swing pipeline. | keep | — | — |
| `GraceNoteDeletion.testDeleteGraceNote` | SELECT mode; click grace note; VK_DELETE → `handleDelete` routes to `removeRange(graceIdx, graceIdx)`; asserts count -1, host preserved. Exercises `handleDelete` on a grace-note selection (non-host) via real keyboard dispatch through Swing pipeline. | yes | §7A: `handleDelete on element selection with contiguous range` → missing unit coverage. Real key dispatch routes through Swing selection + `handleDelete`. | keep | — | — |
| `GraceNoteDeletion.testReplaceHostWithRestRemovesGrace` | In edit mode with REST_ACTION selected, click on host; asserts grace removed (count -1) and replacement is a rest. Exercises `modifyExistingElement` grace-cleanup branch (`!isPitchedNote → removeElement(graceIdx)`). | no | §7C: `modifyExistingElement grace-note cleanup` → missing unit coverage. This is the ONLY coverage for this logic branch; per audit rules, missing unit = keep here — but the risk is pure model logic, not Swing wiring. | keep | The only existing coverage for a missing unit-test branch; mark for unit test during remediation but retain until written. | — |
| `GraceNoteInsertion.testClickClickInsertion` | Grace mode: click at pitch 0 (inserts grace note), then click at pitch -2 (inserts host with glissando); asserts count+2, types, glissando, mode inactive, actions re-enabled. Full two-click grace-note flow through real mouse dispatch. | yes | §6C: click-click insertion → adequate e2e. Toolbar re-enablement after mode exit also exercised. | keep | — | — |
| `GraceNoteInsertion.testDurationChangeDuringFlow` | Grace mode: click grace note, change duration to HALF, click host; asserts grace is QUAVER, host is MINIM, glissando CONNECTED, toolbar selection state. Exercises duration-change during live grace flow via real toolbar click + mouse dispatch. | yes | §6C: duration change during grace flow → adequate e2e. Toolbar `isActionSelected` assertions require real action state. | keep | — | — |
| `InsertElementTypes.testBuildSong` | Setup-only: inserts quarter, half, two eighths, one rest via toolbar clicks + mouse dispatch. No assertions. | no | §7A/§7C: insertion pipeline. No assertions = zero test value regardless of level. | orphan/over-broad | Remove or merge into `testVerifyElementTypesAndAutoBeam` as a @BeforeAll fixture. A test method with no assertions is a defect per the Quality Principles (Correctness). | ✅ |
| `InsertElementTypes.testVerifyElementTypesAndAutoBeam` | Verifies element types/pitches inserted in prior test step; verifies auto-beam span on two eighth notes via `findBeamAt`; verifies rest type and count. Assertions are pure model state queries on `song().getLine(0)`. Auto-beam-on-insert is logic in `PreviewElementManager.applyAutomaticBeaming`. | no | §7C: `insertElement tuplet removal` → adequate unit; auto-beam-on-insert is not enumerated as a matrix row (gap). All assertions are pure model state — bug would live in `applyAutomaticBeaming` logic, not in Swing wiring. Also depends on `testBuildSong` state (stateful sequence anti-pattern). | wrong-level | Split: write unit tests for `insertElement` resulting type/pitch and `applyAutomaticBeaming` span; delete this e2e test. | ✅ |
| `InsertElementTypes.testInsertBetweenAndVerifyShift` | Inserts a note BEFORE an existing element; asserts count+1, new element type and pitch, and that the previously-indexed element shifted right. Exercises `PreviewElementManager.handleClick` insert (not modify) branch via real mouse click before existing element X. | no | §7C: `handleClick forceInsert=true` vs `forceInsert=false` → missing unit; this exercises the normal `forceInsert=false` insert-before path. All assertions are pure model state (element type, staffPosition, count). | wrong-level | Write unit test: mock `LineComponent` with click X landing before an existing element, assert insertion and index shift. | ✅ |
| `InsertElementTypes.testClickOnNoteReplacesIt` | Clicks at existing element's X position at different pitch; asserts element replaced (type CROTCHET, new pitch), count unchanged, mode stays EDIT. Pure model state — exercises `modifyExistingElement` happy path. | no | §7C: `modifyExistingElement decoration preservation` → adequate unit. `mode stays EDIT` assertion is `scoreView().getMode()` — pure state, no Swing dispatch needed. | wrong-level | Write unit test for `modifyExistingElement` pitch-change path. `Mode.EDIT` is assert-able in unit via `ScoreView` mock. | ✅ |
| `InsertElementTypes.testClickWithRestSelectedReplacesWithRest` | REST_ACTION active; click on note; asserts replaced with rest, count unchanged, mode stays EDIT. Exercises `modifyExistingElement` with `editModeManager.makePreviewElement` rest-type conversion. | no | §6C: `makePreviewElement(ElementType)` converts pitched note to rest when REST_ACTION selected → missing unit. This exercises that branch via real toolbar toggle + mouse click. Since unit coverage is missing, this is the only coverage. | keep | Retain until `makePreviewElement` rest-conversion and `modifyExistingElement` rest path have unit coverage. | — |
| `InsertElementTypes.testSameTypeClickKeepsDecorations` | SELECT mode: apply STACCATO to note; re-enter edit mode, click same note position with same duration — asserts staccato preserved on replacement. Exercises `modifyExistingElement` copy-ctor decoration preservation for articulations. | no | §7C: `modifyExistingElement decoration preservation` → adequate unit (`PreviewElementManagerAttachmentTest` covers articulation carry-over). | redundant | `PreviewElementManagerAttachmentTest` already adequately covers articulation preservation; delete this e2e test. | ✅ |
| `FullLine.testInsertIntoFullLineShowsError` | Tries to insert a note into a full line; expects `JOptionPane` error dialog; asserts element count unchanged. Requires real `JOptionPaneFinder` dialog detection after real click through `InsertionSpacingCalculator` + `LineJustificationCalculator`. | yes | §3B: `line-too-full → user-facing error at insert` → adequate e2e. Error dialog only reachable via full insertion pipeline. | keep | — | — |

**Notes:** The `testBuildSong` / `testVerifyElementTypesAndAutoBeam` sequential-state split is a structural defect — `testBuildSong` has no assertions and only exists to build state for `testVerifyElementTypesAndAutoBeam`; together they form an anti-pattern (ordered stateful sequence with a side-effect-only setup step). Both should be converted to a unit test where the fixture builds the line programmatically. `testReplaceHostWithRestRemovesGrace` and `testClickWithRestSelectedReplacesWithRest` are **keep** only because their corresponding unit coverage is matrix-recorded as missing — once unit tests are written for those branches, both should be promoted to **redundant** and deleted. The two `wrong-level` notes in `GraceNoteDeletion` (`testReplaceHostWithPitchedNotePreservesGlissando`, `testClickOnNoteReplacesIt`) could be combined into a single `PreviewElementManager` unit parametrized test during remediation.


### 13C. `NoteConnectionTest` (8) + `DynamicsMarkingTest` (3)

| file · method | behavior | genuine e2e? | matrix coverage (§ · verdict) | verdict | action |
|---|---|---|---|---|---|
| `NoteConnectionTest` · `GlissandoSelection.testClickSelectGlissando` | Click midpoint between two notes; asserts `hasGlissandoSelection()` + selected glissando element index | click real, but the asserted state is pure `LineSelectionState` logic | §7C `LineSelectionState.selectGlissando()` → wrong-level (line 1993, cites this test) | wrong-level | Add unit test for `selectGlissando()`/`hasGlissandoSelection()`; retain only as integration smoke if at all |
| `NoteConnectionTest` · `GlissandoSelection.testSelectSourceNote` | Click source note; asserts `isElementSelected()` + `note.getGlissando() isNotNull` | partial — click real; assertion (b) reads unmodified fixture state | §7C `LineSelectionState.isElementSelected()` → wrong-level (line 1997); §1C fixture read, no test | wrong-level | `getGlissando() isNotNull` is vacuous (fixture never mutated by click, cannot fail); selection assertion belongs at unit level |
| `NoteConnectionTest` · `GlissandoSelection.testSelectTargetNote` | Click target note; asserts `isElementSelected()` + `sourceNote.getGlissando().type == CONNECTED` | partial — click real; `type` assertion reads unmodified fixture state | §7C `LineSelectionState.isElementSelected()` → wrong-level (line 1997) | wrong-level | Both assertions wrong-level; the glissando-type read on an unchanged fixture note cannot fail regardless of click pipeline |
| `NoteConnectionTest` · `GlissandoInsertion.testInsertConnectedGlissando` | Select GLISSANDO_ACTION, click midpoint; asserts glissando present + `type == CONNECTED` | yes — toolbar select + coord→zone + `PreviewElementManager.handleClick` via real dispatch | §7C `PreviewElementManager.handleClick` glissando path → missing (line 2385); §5A glissando-action branch → missing | keep | Primary integration smoke for the glissando insertion pipeline (sole coverage of a missing unit path) |
| `NoteConnectionTest` · `GlissandoInsertion.testInsertSlideOut` | Select SLIDE_OUT_ACTION, click midpoint; asserts glissando present + `type == SLIDE_OUT` | yes — same pipeline, SLIDE_OUT variant of `computeGlissandoZone` | §7C `handleClick` glissando path → missing; SLIDE_OUT is a distinct zone branch | keep | Covers the SLIDE_OUT zone branch; not redundant with the CONNECTED case |
| `NoteConnectionTest` · `GlissandoDeletion.testDeleteSelectedGlissando` | Select glissando by click, press DELETE; asserts `getGlissando() isNull` | yes — real key event drives `ScoreViewController.handleDelete` glissando branch | §7A `handleDelete` on glissando selection → missing (line 2244) | keep | Sole coverage of the glissando-selection delete routing |
| `NoteConnectionTest` · `GlissandoDeletion.testDeleteSourceNoteRemovesGlissando` | Click source note, press DELETE; asserts count −1 + shifted target glissando cleared | yes — real key event; `handleDelete` element routing → `deleteNote` | §7A `deleteNote` glissando strip → adequate (line 2234); `handleDelete` element path → missing (line 2242) | keep | The `deleteNote` cleanup itself is unit-adequate; kept for the still-missing `handleDelete` element-routing path |
| `NoteConnectionTest` · `GlissandoDeletion.testDeleteTargetNoteRemovesGlissando` | Click target note (shifted), press DELETE; asserts count −1 + source glissando cleared | yes — real key event; symmetric target-deletion case | §7A `handleDelete` element path → missing (line 2242) | keep | Same rationale; sequential fixture state is genuine integration state |
| `DynamicsMarkingTest` · `Regression.testCrescendoStillWorks` | Shift-click range, ADD_CRESCENDO_ACTION; asserts crescendo span covers range | yes — synthetic shift-click + real action dispatch through crescendo pipeline | §1B `Line.addCrescendo` merge → wrong-level (line 227, "e2e smoke fine to keep"); §5C → adequate/keep (line 1674) | keep | Matrix §1B/§5C both record keep; shift-click selection wiring is genuine integration |
| `DynamicsMarkingTest` · `Regression.testDiminuendoStillWorks` | Shift-click range, ADD_DIMINUENDO_ACTION; asserts diminuendo span covers range | yes — same dispatch pipeline, diminuendo hairpin type | §5C diminuendo → adequate/keep (line 1675) | keep | Parallel to crescendo; matrix records keep |
| `DynamicsMarkingTest` · `Regression.testFermataAndDynamicCoexistOnSameNote` | Apply FERMATA_ACTION + DYNAMIC_F menu item; asserts fermata active + `DynamicAttachment.type == FORTE` | partial — toolbar/menu dispatch real; assertions are model state | §5C dynamic+fermata coexist → adequate/keep (line 1673) | keep | Matrix records adequate/keep; guards the "dynamics refactor broke fermata coexistence" regression |

**Notes.** `DynamicsMarkingTest` carries a **class-level orphan** condition (not a per-method verdict): its javadoc claims "Covers serialization (E6)" and the base-class `roundTrip` helper is never called — stale residue of removed serialization tests; with a single nested class the `@TestClassOrder` + `@Order(5)` are no-ops. The three surviving `@Test` methods are individually sound (all keep). The `GlissandoSelection` trio has a compounding quality defect on top of wrong-level: `testSelectSourceNote`/`testSelectTargetNote` each assert unmodified fixture model state (`getGlissando() isNotNull`, `.type == CONNECTED`) that cannot fail regardless of click-pipeline health.

**Tally (recomputed from verdict column): keep 8 · wrong-level 3 · redundant 0 · orphan 0.**

